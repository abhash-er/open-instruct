# IF mixture: 100% English (Dolci replay), 0% EU (synthetic), 1M samples
# EU split equally across 11 langs (~0 samples/lang).
# EU source: synthgen-if (synthetic). English source: dolci-replay (~1,000,000 samples).
# Real EU sources to be mixed in later experiments.

name: synthif-1M-100en
total_samples: 1000000
english_ratio: 1.0

sources:
  - synthgen-if
  - dolci-replay

seed: 42
